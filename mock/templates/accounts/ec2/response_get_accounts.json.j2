[
    {% for n in range(random.int(2, 5)) %}
    {
        "additional_regions": [],
        "group": {
            "id": "3",
            "name": "{{ ['image', 'container', 'vm'] | random}}-{{['release', 'test', 'stage'] | random }}"
        },
        "id": "{{ random.int(4000, 100000) }}",
        "name": "{{ ['sale', 'resale', 'test'] | random }}-{{ ['images', 'containers'] | random }}-{{ fake.country_code() | lower }}"
        "partition": "aws{{ ['', '-us-gov', '-cn' ] | random }}",
        "region": "{{ fake.country_code() | lower }}-{{ ["north", "east", "west", "south"] | random }}-{{ random.int(1,3)}}",
        "subnet": "subnet-{{ fake.hexify(text='^^^^^^^^')}}"
    }{%- if not loop.last %},{% endif %}{% endfor %}
]