[
    {% for n in range(random.int(2, 5)) %}
    {
        "bucket": "{{['test', 'production', 'new'] | random }}-{{ ['images', 'containers', 'distributions'] | random }}-bucket",
        "id": "{{ random.int(4000, 100000) }}",
        "name": "{{ ['sale', 'resale', 'test'] | random }}-{{ ['images', 'containers'] | random }}-cn"
        "region": "{{ fake.country_code() | lower }}-{{ ["north", "east", "west", "south"] | random }}-{{ random.int(1,3)}}",
        "security_group_id": "sg-{{ fake.hexify(text='^^^^^^^^')}}",
        "vswitch_id": "vsw-{{ fake.hexify(text='^^^^^^^^')}}",
    }{%- if not loop.last %},{% endif %}{% endfor %}
]